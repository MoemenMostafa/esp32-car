import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.moamen.esp32car',
  appName: 'esp32-car',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
